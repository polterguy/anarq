.arguments
   id:long
.description:Deletes one item from your posts table in your [generic|anarq] database matching the specified argument(s)
.type:crud-delete
validators.mandatory:x:@.arguments/*/id
auth.ticket.verify:root, admin
data.connect:[generic|anarq]
   database-type:mysql
   add:x:+/*/where/*/and
      get-nodes:x:@.arguments/*
   data.delete
      database-type:mysql
      table:posts
      where
         and
   unwrap:x:+/*
   return
      affected:x:@data.delete
